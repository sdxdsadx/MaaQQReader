# QQR-33 广告修复记录（2026-09-10）

Issue：https://linear.app/sdxdsadx/issue/QQR-33

当前项目为 `G:\project_X` 的新版模块化工程，修复基线为 `f4b7387`。旧工程位于 `_backup_old_project_20260909_200716`，不是当前 GUI 的广告运行器。

## 需求依据

- QQR-27/28：入口、滚动、退出后误入书架/阅读页。
- QQR-30：加速、继续观看与完成弹窗。
- QQR-31/32：直播浏览每 5 秒滑动，默认 8 次后退出；QQR-32 已取消剩余 2 秒提前退出。
- QQR-14：最终以广告卡 `12/12` 或“明日再来”验收，Linear 仍为 Backlog。
- 用户本轮追加：记录 Issue 与修正；退出广告可能连退两次到书架。

## 复现与修正

| 故障证据 | 原因与修正 |
| --- | --- |
| 历史 `runtime/logs/qqr27_ad_run10.log` 连续点击 `(600,1078)` 直至超时 | 定位缓存跨帧保存成功/失败；改为当前截图身份变化时失效，停止用固定坐标补点 |
| 实跑奖励页抽奖悬浮框挡住观看按钮，OCR 只见“立民”，另有“立即分享” | 单字“立”不能定位观看按钮；改为滑动卡片后重观察 |
| 实跑 OCR：“直播中 / 进入直播间 / 需要下滑浏览更多才能领取奖励哦 / 23秒”，倒计时停住 | 状态识别依赖“广告”标签，直播分支不可达；补直播、加速与弹窗 OCR 降级识别 |
| 广告素材含“活动”时临时被判 GAME_HALL | 从游戏大厅身份的泛化候选中移除单独“活动”；其他游戏大厅特征保持 |
| 广告卡与游戏卡同屏被判 GAME_ENTRY，随后不推进 | 广告任务将 GAME_ENTRY 视为同一奖励页中的任务行，按广告卡导航，不能进入游戏 |
| 第二轮广告不再等待 40 秒；直播背景挡住“继续观看/去领取奖励”等动作 | 返回奖励页重置每轮状态；直播弹窗动作先于滑动；退出尝试不立即重置为新一轮 8 次滑动 |
| 实跑连续“点击 X → 仍见退出动画 → 返回键 → 奖励页 → 书架” | 退出是异步过程；退出动作后等待 3 秒再重观察，禁止在动画期间补发返回；已回奖励页则进入奖励页分支 |

## 验证

- 新增最初 8 项跨帧/跨轮回归在修复前全部失败，修复后通过。
- 继续增加实际 OCR、遮挡、游戏行共存、验证码优先拦截与退出动画回归；最新全量命令 `D:\python\python.exe -m pytest -o addopts='' -q`：**206 passed**。
- 实跑命令：`D:\python\python.exe scripts/run_task.py --config configs/qqreader.local.json --task DailyAdFlow --timeout-minutes 3`。
- 首轮定位直播漏识别；第二轮定位遮挡和奖励页游戏行分支；定位明确后按本轮精确 PID 停止测试进程再修改。
- 补退出等待前，实跑已能滑动到“奖励已发放～”，退出并再次找到下一条广告；但发生两次退出及验证码阻塞，奖励卡仍为 `0/12`，**不是业务成功**。证据：`runtime/records/DailyAdFlow_19700103_063435_265000.json`。
- 最新退出等待版本实跑：从书架自行进入奖励页并找到广告；直播倒计时从 30 秒推进到“奖励已发放～”；8 次滑动后只点击一次 X，返回奖励页后等待剩余 2.109 秒，未再返回书架。随后验证码阻塞，结果为 `BLOCKED_BY_CAPTCHA`，记录为 `runtime/records/DailyAdFlow_19700103_063655_531000.json`；奖励卡仍为 `0/12`，不宣称计数结算或整轮成功。
- 用户要求滑动验证器另立 [QQR-34](https://linear.app/sdxdsadx/issue/QQR-34)（Backlog），本轮不修改求解器。206 项全量测试通过；`dist/QQReaderGUI.exe` 已重新构建，GUI 后续启动广告任务会使用修正后的脚本。

运行记录的 `19700103` 文件名前缀来自现有单调时钟被作为日期输出，不是实际运行日期；本记录以文件修改时间、MAA 日志和本轮命令核对。此记录日期问题未在广告修复中顺手改动。
